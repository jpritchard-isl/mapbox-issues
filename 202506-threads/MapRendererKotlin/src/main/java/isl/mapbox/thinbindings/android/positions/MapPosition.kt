package isl.mapbox.thinbindings.android.positions

// The position on the map of a feature (usually when selected by an operator).
public class MapPosition (val GeoPosition: GeographicPosition, val ScreenPosition: ScreenPosition) { }